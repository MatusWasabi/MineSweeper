
// Enum can be used here but as I used this in .Net C# as well. I believe it is safer-approach.
// Using Enum introduces risk of unintentional bit-wise operation.
public static class CellMarkType
{
    public const int BombMark = -1;
}
